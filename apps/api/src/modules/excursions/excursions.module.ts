import { Module } from '@nestjs/common';

/**
 * Módulo `excursions` (excursão, estados, pontos de embarque, fotos).
 * Bootstrap (item 1.0): só a estrutura e o schema (schema.ts) existem ainda.
 * Controllers/services chegam nas histórias H1.5–H1.7, seguindo
 * `docs/api/excursions.yaml`.
 */
@Module({})
export class ExcursionsModule {}
