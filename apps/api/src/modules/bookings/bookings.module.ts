import { Module } from '@nestjs/common';

/**
 * Módulo `bookings` (passageiro, reserva, mapa de poltronas, embarque).
 * Bootstrap (item 1.0): só a estrutura e o schema (schema.ts) existem ainda.
 * Controllers/services chegam nas histórias H1.8–H1.13, seguindo
 * `docs/api/bookings.yaml`.
 */
@Module({})
export class BookingsModule {}
