/**
 * PLACEHOLDER mínimo — o dashboard real (KPI da próxima excursão, atalho de
 * embarque) chega com o módulo excursions. Existe como rota real dentro do
 * AppShell (não mais o placeholder de dev da fundação).
 */
export function InicioPage() {
  return (
    <div className="tt-page-placeholder">
      <h1>Início</h1>
      <p>O painel com sua próxima excursão chega em breve.</p>
    </div>
  );
}
